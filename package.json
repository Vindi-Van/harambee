{
  "name": "harambee",
  "private": true,
  "version": "0.1.0",
  "description": "Harambee orchestration framework",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @harambee/policy build",
    "test": "npm run -w @harambee/policy test",
    "check": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
